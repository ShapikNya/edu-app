<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ControlTemplate x:Key="Answer" TargetType="Button">
        <Grid Width="227" Height="35" Background="GhostWhite" HorizontalAlignment="Left"
              Margin="95,0,0,0">
            <Border BorderThickness="1" BorderBrush="#35082C" Padding="7,5,5,5"> 
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1*"/>
                        <ColumnDefinition Width="1*"/>
                    </Grid.ColumnDefinitions>
                        <TextBlock Text="Your value:" Grid.Column="0">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="HorizontalAlignment" Value="Center"/>
                                <Setter Property="VerticalAlignment" Value="Center"/>
                                <Setter Property="FontSize" Value="14"/>
                                <Setter Property="Foreground" Value="#2D3E5F"/>
                                <Setter Property="FontFamily" Value="Output Sans, Helvetica, Arial, sans-serif"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                                <Setter Property="HorizontalAlignment" Value="Left"/>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>

                    <Label Grid.Column="1" Content="{TemplateBinding Content}">
                        <Label.Style>
                            <Style TargetType="Label">
                                <Setter Property="Width" Value="auto"/>
                                <Setter Property="FontSize" Value="14"/>
                                <Setter Property="Foreground" Value="#1A1A1A"/>
                                <Setter Property="FontFamily" Value="Output Sans, Helvetica, Arial, sans-serif"/>
                                <Setter Property="VerticalAlignment" Value="Center"/>
                                <Setter Property="Margin" Value="0,0,0,0"/>
                                <Setter Property="HorizontalAlignment" Value="Right"/>
                            </Style>
                        </Label.Style>
                    </Label>
                </Grid>
            </Border>
        </Grid>
    </ControlTemplate>
</ResourceDictionary>
    